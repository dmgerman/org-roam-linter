# Implement the following plan

Follow the `Instructions` to implement the `Plan` then `Report` the completed work.

## Instructions

1. **Read the plan**
   -  Think hard about the plan and implement the plan.

2. **Make sure you understand the plan**. 
   - Otherwise ask questions.

3. **DO NOT COMMIT any changes**

4. **Log interaction with the User**
   - Create a file for logging user interactions in markdown format with the same name as the plan (in the same directory) with appended "-implementation-<date>" to the filename (before .md). The date format should be YYYY-MM-DD. 
   - After every interaction, append both the user message and your response to the log file in chronological order.
   - Always keep logging automatically without being asked.

## Coding standards

- Use production ready code
- Write elegant solutions, not verbose ones
- DRY principle strictly enforced (avoid cloning)
- Make sure functions are concise and follow the current naming standards of the project
- Where applicable, use well-known idioms and functional programming. Avoid mutation whenever possible.

## Plan

$ARGUMENTS

## Report

- Summarize the work you've just done in a concise bullet point list.
- Report the files and total lines changed with `git diff --stat`
- Create a file with this summary and report (in org-mode format) with the same name as the plan (in the same directory) with appended "-completed-<date>" to the filename (before .org). The date format should be YYYY-MM-DD. 
